[Unit]
Description=LiteLLM proxy (Anthropic → NVIDIA NIM fallback)
After=network.target

[Service]
Type=simple
EnvironmentFile=%h/litellm/.env
ExecStart=/usr/bin/python3 %h/litellm/nim_proxy.py
Restart=on-failure
RestartSec=10

[Install]
WantedBy=default.target
