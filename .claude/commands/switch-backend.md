Switch the Claude Code agent backend between Anthropic and NVIDIA NIM (DeepSeek V4 Pro).

Run `~/bin/switch-backend $ARGUMENTS` via the shell tool, then show the result.

Valid arguments: `anthropic`, `nvidia`, `status`
